# MQTT Broker Configuration (HiveMQ)
MQTT_BROKER_URL=mqtt://localhost:1883
MQTT_USERNAME=jrg
MQTT_PASSWORD=your_password_here

# Z-Wave JS UI Configuration
# IMPORTANT: Even though this is running locally on the Pi, you MUST enable
# authentication to prevent unauthorized access to your Z-Wave network.
# Set DEFAULT_USERNAME and DEFAULT_PASSWORD in your ZWaveJSUI deployment,
# then set ZWAVE_UI_AUTH_ENABLED=true and provide credentials below.
ZWAVE_UI_URL=http://localhost:8091
ZWAVE_UI_USERNAME=admin
ZWAVE_UI_PASSWORD=change_this_password

# Enable authentication (RECOMMENDED: set to 'true' for production)
# When false, anyone with network access can control your Z-Wave devices
ZWAVE_UI_AUTH_ENABLED=true

# Optional: Socket connection timeout in milliseconds (default: 5000)
# ZWAVE_UI_SOCKET_TIMEOUT_MS=10000
